/* tslint:disable:no-unused-variable */

import {GlobalUtilsComponent} from "./global-utils.component";

describe('Component: GlobalUtils', () => {
  it('should create an instance', () => {
    let component = new GlobalUtilsComponent();
    expect(component).toBeTruthy();
  });
});
