/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ModificaMovimentoComponent } from './modifica-movimento.component';

describe('Component: ModificaMovimento', () => {
  it('should create an instance', () => {
    let component = new ModificaMovimentoComponent();
    expect(component).toBeTruthy();
  });
});
