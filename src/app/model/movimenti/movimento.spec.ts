/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {Movimento} from './movimento';

describe('Movimento', () => {
  it('should create an instance', () => {
    expect(new Movimento()).toBeTruthy();
  });
});
